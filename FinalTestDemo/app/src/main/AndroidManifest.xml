<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.finaltestdemo">

    <uses-permission android:name="android.permission.RECEIVE_SMS" />
    <uses-permission android:name="android.permission.READ_SMS" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.WRITE_SMS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".createBroadcast.ReceiveSentBroadcast"></activity>
        <activity android:name=".createBroadcast.NormalBRActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <receiver android:name=".orderedBroadcasts.ReceiveOrderedBroadcast1">
            <intent-filter>
                <action android:name="com.example.finaltestdemo.orderedBroadcasts"/>
            </intent-filter>
        </receiver>
        <receiver android:name=".orderedBroadcasts.ReceiveOrderedBroadcast2">
            <intent-filter>
                <action android:name="com.example.finaltestdemo.orderedBroadcasts"/>
            </intent-filter>
        </receiver>
        <activity android:name=".MainActivity" />
    </application>

</manifest>